# JUMP 工作约定

用户于 2026-09-14 批准新分支、新目录、开始资格实验并显著扩大预算。新主线是持续前向行驶下窄障碍完整腾越的准备与触发联合决策。本目录的研究契约替代父目录针对旧包线任务的固定起点和首次落地终点；旧 JIT 原件不修改。

- 唯一当前计划与进度为 PROJECT.md，验证为 docs/VALIDATION.md，方法和实验简表为 docs/METHODS_AND_RESULTS.md。
- 所有新源码、配置、报告与运行输出放 JUMP；runs 不提交。不可读写 STTW_CONTROL；旧 JIT/src 作为固定版本依赖，旧 checkpoint 只读引用并绑定哈希；不得打开旧 final TEST。
- 模型使用原机器人和 2kg 载荷、原伺服/摩擦/力矩限制；场景障碍显式配置。sim_dt=.005/control_dt=.020。动作顺序 steer/drive/hip/knee，分别位置/轴速/位置/位置。
- 触发命令与实际离地不同。完整任务包含前向、禁止碰撞、真实离地、全车通过、两轮合法落地及 2s 恢复观察；最后 .5s 连续跟踪达标。未知不能作负标签。
- 先资格、再窗口与机制、再学习上层。底层变化必须另立阶段，所有对照共同冻结；当前资格测试不代表旧 MJX 结果回放等价。
- 宏观预算 200,000,000 控制步，分配见 configs/campaign.json；不是必须花完。每次运行先声明本次上限、角色、停止条件、代码/配置/模型/checkpoint 身份；实际计费和预算分开。
- 保留完整轨迹、真实失败终点、逐工况结果、PNG/PDF和索引。所有实验启动错误和完成桌面 watcher，核对心跳与送达；不能终止其他项目进程。
- 保留用户修改；每个大任务相关测试和 diff 审查后显式 stage、commit、push 到 agent/jump-preparation-timing，不重复请求同一授权。
