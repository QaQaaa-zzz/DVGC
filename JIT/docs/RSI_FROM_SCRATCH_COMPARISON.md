# 从探索见证重新训练 RSI 策略并配对评估

用户授权：原奖励、全新策略训练100万步预算；最新指令改用`lineage_repair_0010`作为对照，不再使用项目原始π0。比较无扰动轨迹及100个随机扰动回合。

## 冻结设计

- 训练支持来自 `runs/analysis/liftoff_aligned_20260918` 对齐图选中的成功候选，恢复原始完整快照，不平移物理状态。
- 全新Actor/Critic/优化器/归一化统计，不恢复旧checkpoint。模板策略仅提供模型、观测、动作、时序和原奖励配置。
- 沿用20%固定x=2.5起点、80%完整快照RSI，phase平衡；仅TRAIN支持，不打开final TEST。
- PPO按3200步整批执行，1000000上限内训练998400步（312批），不向上超预算。末checkpoint作为预先声明对照，不按评估结果挑选。
- 末训练检查最多1600交互；两策略分别1个无扰动回合和100个配对随机动作残差回合，每回合最多400控制步。脉冲3步，四通道各均匀[-0.25,0.25]，起扰0/5/10/15/20/25步均衡混合；请求序列一致，实际限幅值保存。
- 固定初态执行单个策略完整闭环，不在候选处切换或重放。成功采用相同stable_forward_recovery；失败、超时、冲突分别保存。早失败或未到扰动时刻仍计入100回合分母。
- 图包含世界坐标、离地点对齐、两策略无扰动轨迹、全部扰动轨迹及成功率。未离地失败保留在世界坐标图及结果表。
- 正式预算仅一次，不自动延长；保留日志、配置哈希、桌面错误/完成监视器。

## 检查表与进度

- [x] 冻结支持与lineage_repair_0010身份：1368个完整快照，上升1139、下降229。
- [x] 增加显式fresh初始化及行为测试，旧默认保持。
- [x] 增加随机短脉冲后继续到终点的完整回合评估及测试。
- [x] 冻结配置/预算/代码，启动并核验监视器：f72e383，监督390695、训练390862、监视390696；启动通知返回0。
- [x] 完成训练、配对评估、轨迹与扰动核验、出图。
- [x] 报告、提交和推送（以最终Git交付回执为准）。

当前：39项相关CPU测试通过，1368状态零交互预检通过，配置已生成在
`runs/experiments/fresh_rsi_comparison_20260918/`。总交互上限1080800，
含训练998400、末检查1600、两策略各101回合共80800。已启动并确认实际采样更新；实时状态以实验目录为准。
其他GPU任务不停止；启动各子任务前要求GPU空闲显存≥8GiB、主机可用内存≥6GiB，
最多等待1800秒，每子任务最多7200秒，不自动重试。不把争用墙钟当性能证据。

独立代码复核发现环境超时应与外层horizon耗尽分别统计。源码报告已改为读取原始
end_code并保存终止原因；冻结运行代码不改，完成后从原始记录生成新派生核验报告，
不重训或重评。成功率分母不受此修正影响。

## 已完成结果

[全部产物与对比图](../runs/experiments/fresh_rsi_comparison_20260918/INDEX.md)。
基线无扰动成功，随机扰动78/100；全新RSI无扰动3步stuck，随机扰动0/100，
全部100次为stuck，其中84次尚未到计划起扰时刻。因此是固定起点能力未学成，
不能解释为100次被扰动击倒，也不能据单种子断言RSI普遍无效。
基线其他终止为roll_limit16、yaw_limit5、timeout1。

998400训练步、167末TRAIN检查步、80800含同步填充的评估计费步，共1079367，
低于1080800上限。transition_0 Actor哈希与基线不同，归一化计数为零；
末checkpoint恢复通过，随机draw与共同存活期请求扰动逐元素配对一致。

原冻结流水线在最后INDEX写入时被同名baseline元数据覆盖统计字典触发错误；
40项测试（含完整报告回归）通过，派生comparison_verified已重建并检查图面。
原status错误保留，ACTIVE_RUN改指核验完成状态，零额外训练/评估交互。

## 2026-09-18 指定 Phase U 奖励的全新 RSI 800万步

用户指定 `phase_u_v4_speed2_roll400_missed200_9977856_seed820701_20260826/checkpoints/transition_4988928` 为奖励来源。
实际恢复检查通过：训练计数4988928，配置哈希 `baae0efb9be3d35daad6ecfd0a6e36afdbf3aecc9d2f45f4699e0eca6997284d`。
其冻结 resolved_config 已是前次998400步RSI的上游奖励来源，因此没有更换奖励数值。
仍以 original_all_phases 将原Phase U公式用于完整RSI回合，采用stable_forward_recovery终点；
这不同于原模型仅训练上升阶段的任务范围，不宣称重现其整个训练实验。

新Actor/Critic/优化器/归一化从零初始化，不加载指定模型或前次RSI参数。
同一1368原始完整快照，20%固定起点、80%RSI，保留原PPO配置及种子9182601。
预算8000000步（2500整批），末TRAIN检查最多1600步，后续基线lineage_repair_0010和新策略
各1无扰动+100配对随机脉冲回合最多80800步，总上限8082400；不自动续训。

独立目录 [实验索引](../runs/experiments/fresh_rsi_phase_u_reward_8m_20260918/INDEX.md)，
含奖励来源审计、完整参数、零交互1368状态预检及输入哈希锁。
冻结代码0ccb2ee，监督PID457072、训练PID457447、监视PID457073；已确认实际采样至少240000步，结果尚未完成。
实时进度以该目录status.json和training/fresh_rsi/status.json为准。前次负结果和产物保留。

## 三策略配对扩展（2026-09-18）

按用户新指令增加原Phase U transition_4988928。复用完成的800万步实验两组轨迹，
只新增原模型100随机扰动+1无扰动回合，上限40400控制步，零训练。
实现计划：原位扩展rsi_comparison和薄CLI；限定fixed_random完整回合才允许
直接加载原Phase U Actor/归一化/critic，核验原配置、XML、动作/观测与参数哈希；
相同运行模板、固定起点和稳定恢复终点，不切专家。报告接受声明的方法列表与数据路径，
逐元素验证随机draw和共同存活扰动；保留全部失败并输出三策略同图PNG/PDF/SVG及CSV。
原Phase U只针对上升训练，因此完整恢复评价是扩展任务诊断。

复用入口：`cli/run_rsi_comparison.py prepare-phase --previous <已完成实验> --checkpoint <原Phase U checkpoint> --output <新目录>`，
随后使用既有launch命令冻结代码并启动监督与桌面监视器。
本次目录：`runs/experiments/three_policy_pulse_comparison_20260918`。
测试覆盖三方法分母、随机draw不配对拒绝、学习模式禁止覆盖、原载荷保持及配置漂移拒绝。

## 2026-09-18 三策略100回合配对扰动已完成

800万步RSI训练已完成（8000000训练+386末TRAIN检查）；原Phase U transition_4988928
新增100随机扰动+1名义回合，与同一批lineage_repair_0010和RSI轨迹合图。
基线76/100、RSI72/100、Phase U66/100；三者无扰动均成功，根部峰高分别0.534/0.529/0.677m。
原Phase U更高不等于稳定恢复更好；RSI在本次开发样本未超过lineage_repair_0010。
全部300回合收到非零扰动且检出诊断离地，全部失败保留。随机draw及共同存活请求逐元素配对。

新增预算/计费40400（有效6822），复用80800，零训练；评估冻结代码196c27e。
两子进程退出0，桌面完成通知交付成功；原载荷Actor/critic/normalizer不变，独立保留Phase U来源，
不将共享运行模板当作其训练身份。报告CSV区分零幅值名义窗口与实际非零扰动，零仿真重建comparison_verified。
44项相关CPU检查通过（16扩展/脉冲与28训练回归），已核验PNG图面、输入和冻结代码哈希。
[三策略图与数据](../runs/experiments/three_policy_pulse_comparison_20260918/INDEX.md)。

## 扩展到每策略10000回合（2026-09-18，用户授权）

目的：固定这三个checkpoint，扩大同分布随机动作脉冲的配对开发样本；不重新训练。
每策略10000，共30000新回合，horizon400，最大计费12000000控制步。
方案：每批256（末批16），40批；各批三策略同seed及全局回合偏移，批间seed不同。
沿用四通道±0.25、3步、0/5/10/15/20/25起扰以及稳定恢复判据；原100回合保留独立，
名义轨迹复用已验数据。分批是显存管理，不称与一次10000并行逐位等价。

实施顺序：测试批次完整覆盖/种子配对/起扰余数分配→共享采集器分批调度与有界成本记录→
逐批检查随机draw、请求、有效步、真实终点→逐回合CSV/批次NPZ/哈希索引/全部轨迹合图→
冻结代码启动并验证首批、桌面监视器。异常即停，无自动重试或自动增加回合。
每子任务最多7200秒、资源等待最多1800秒；保留其他任务，沿用8GiB GPU/6GiB主机门槛。
绘图逐批读取，轨迹层栅格化保留全部回合，PDF/SVG坐标与文字可编辑；不以抽样图代替完整数据。

启动核验：冻结代码b596a6b，22项相关CPU检查通过；首批三者各256回合完成、配对验证通过。
监督712237、监视712238；当前进度与全部数据见`runs/experiments/three_policy_pulse_10k_20260918/INDEX.md`。

## 假设性恢复重标注（2026-09-18，零新增仿真）

用户指定把基线终点x=4.0–4.5m、已有效接触的正式失败中，保持1–12步的102回合全部计成功，并把保持0步的730回合的一半计成功。后者使用固定seed哈希顺序确定性选出365回合。由此基线假设成功率为7,831/10,000（78.31%）；RSI和Phase U保持正式统计77.46%与76.89%。独立图不画失败红叉，显示截止x=5.5m。该结果只用于查看替代口径的影响，不替代正式25步稳定恢复判据。

[图、逐回合重标注与规则](../runs/experiments/three_policy_pulse_10k_20260918/comparison_counterfactual_partial12_halfzero_v2/INDEX.md)。
