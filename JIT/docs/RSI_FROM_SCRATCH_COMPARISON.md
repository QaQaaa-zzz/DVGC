# 从探索见证重新训练 RSI 策略并配对评估

用户授权：原奖励、全新策略训练100万步预算；最新指令改用`lineage_repair_0010`作为对照，不再使用项目原始π0。比较无扰动轨迹及100个随机扰动回合。

## 冻结设计

- 训练支持来自 `runs/analysis/liftoff_aligned_20260918` 对齐图选中的成功候选，恢复原始完整快照，不平移物理状态。
- 全新Actor/Critic/优化器/归一化统计，不恢复旧checkpoint。模板策略仅提供模型、观测、动作、时序和原奖励配置。
- 沿用20%固定x=2.5起点、80%完整快照RSI，phase平衡；仅TRAIN支持，不打开final TEST。
- PPO按3200步整批执行，1000000上限内训练998400步（312批），不向上超预算。末checkpoint作为预先声明对照，不按评估结果挑选。
- 末训练检查最多1600交互；两策略分别1个无扰动回合和100个配对随机动作残差回合，每回合最多400控制步。脉冲3步，四通道各均匀[-0.25,0.25]，起扰0/5/10/15/20/25步均衡混合；请求序列一致，实际限幅值保存。
- 固定初态执行单个策略完整闭环，不在候选处切换或重放。成功采用相同stable_forward_recovery；失败、超时、冲突分别保存。早失败或未到扰动时刻仍计入100回合分母。
- 图包含世界坐标、离地点对齐、两策略无扰动轨迹、全部扰动轨迹及成功率。未离地失败保留在世界坐标图及结果表。
- 正式预算仅一次，不自动延长；保留日志、配置哈希、桌面错误/完成监视器。

## 检查表与进度

- [x] 冻结支持与lineage_repair_0010身份：1368个完整快照，上升1139、下降229。
- [x] 增加显式fresh初始化及行为测试，旧默认保持。
- [x] 增加随机短脉冲后继续到终点的完整回合评估及测试。
- [x] 冻结配置/预算/代码，启动并核验监视器：f72e383，监督390695、训练390862、监视390696；启动通知返回0。
- [ ] 完成训练、配对评估、轨迹与扰动核验、出图。
- [ ] 报告、提交和推送。

当前：39项相关CPU测试通过，1368状态零交互预检通过，配置已生成在
`runs/experiments/fresh_rsi_comparison_20260918/`。总交互上限1080800，
含训练998400、末检查1600、两策略各101回合共80800。已启动并确认实际采样更新；实时状态以实验目录为准。
其他GPU任务不停止；启动各子任务前要求GPU空闲显存≥8GiB、主机可用内存≥6GiB，
最多等待1800秒，每子任务最多7200秒，不自动重试。不把争用墙钟当性能证据。

独立代码复核发现环境超时应与外层horizon耗尽分别统计。源码报告已改为读取原始
end_code并保存终止原因；冻结运行代码不改，完成后从原始记录生成新派生核验报告，
不重训或重评。成功率分母不受此修正影响。
