# Downstream Hip-Positive Boundary-Completion Plan

**Goal:** Execute one 25-attempt evidence-directed panel to complete downstream
TRAIN readiness or close strength-only acquisition.

## Tasks

- [x] Add RED tests for the exact targeted mode, action/sign/strength/duration,
  prior evidence, budgets, and protocol purpose.
- [x] Extend the existing validator with this one locked targeted variant while
  preserving both previous modes exactly.
- [x] Add the exact config and prelaunch declaration with a new output path and
  seed namespace.
- [x] Run static, focused CPU/GPU, full preflight, and enhanced audit gates
  (35 focused CPU, 4 focused GPU, 420 full CPU, and 14 full GPU passed).
- [ ] Explicitly stage/commit only validated JIT paths and re-audit at the
  committed HEAD.
- [ ] Launch exactly one panel and delegate sparse monitoring/result analysis
  to Luna medium.
- [ ] Freeze Iteration-0 TRAIN evidence and design group-disjoint validation if
  ready; otherwise stop the strength-only route.
