Edit reference image 1, the complete four-panel JIT method figure. Reference image 2 is ONLY a morphology reference from the actual JIT simulator; ignore its background, graphs, numbers and telemetry. Preserve image 1's four-panel composition, academic style, mostly excellent readable typography, colors, equations, explanatory details, and main single-jump tube. Produce a refined landscape figure, preferably 3840 x 2560 pixels with sharper large text. Do not add any experimental result or progress annotation.

Make these precise scientific corrections. It is acceptable to shift boxes modestly and widen gutters to make the corrected arrows legible. Prioritize truthful directed connections over fitting more decorative objects.

1. Replace EVERY small robot illustration in panels (a) and (c) and on the pulse-event trajectory with a simplified side/three-quarter drawing of the SAME two-wheeled robot in reference image 2. It has two visibly SEPARATED inline wheels at the front and rear, a compact narrow rectangular body bridging them, a slender front steering fork, and a two-link articulated hip/knee pendulum mechanism rising above the body, with a small elongated upper mass. The two wheels have different forward x positions and must be plainly visible; it is NOT a Segway, not a unicycle, not a coaxial-wheel robot. It is not a quadruped. Keep the actual mechanism silhouette and approximate proportions visible in reference image 2. Use restrained gray metal with muted copper/pink accents. Do not insert the telemetry or floor screenshot itself.

2. The main action composition in panel (a) is already essentially right; preserve it: current policy -> plus, residual explorer -> event/amplitude gate -> plus -> action clip -> actuator mapping/MuJoCo. Preserve separate 76-D actor observation and additional 30-D privileged input to the explorer. The feedback line "Next observation" must start at the MuJoCo dynamics output and end at Observation + history. Remove any dangling line that appears to feed an unexplained input into the summation circle. The only two summation inputs are the base action and the gated/scaled residual.

3. Correct witness and pending DATA FLOW in panel (b). The Yes output of current-pi continuation goes into Successful witnesses W_i. The No output goes into Eligible pending candidates P_i. Unknown remains separate. The successful-witness cylinder must NOT have a downward arrow feeding the candidate re-test box. Instead send W_i through a clearly labelled right-side/downward route directly to the tube/archive in (d), label "Add verified trajectories". Pending P_i must feed the "Old successful starts + new pending starts" training buffer in (c), with an amber/dark arrow routed through the central gutter labelled "New training starts"; it must not merely point to the re-test box. Add a teal old-support input to that same mixed-reset buffer labelled "Old successful starts". Pending states can also be referenced by the re-test box through a small port labelled "Same saved s*".

4. Correct the refinement/re-test cycle. "PPO fine-tuning" in (c) produces "Candidate π′". That output must actually feed "Re-test the SAME s* with candidate π′" in (b); use a labelled violet dashed port-to-port route if a continuous long line would cross text. Inside the re-test inset, arrows must run Candidate π′ -> Continue from s* (pulse OFF) -> Jump + stable landing? The YES output returns upward into Successful witnesses W_i. The NO output enters "No witness within training budget", which then connects to the Visited ledger. The Visited ledger should retain all visited states; describe failed states as retained, never mathematically impossible. The re-test's outcomes must ALSO feed the policy-acceptance gate in (c), via a clearly labelled route "New capability / re-test results".

5. Correct the policy acceptance path in (c): Candidate π′ -> diamond "New capability + old-skill retention + valid nominal jump?" -> Yes -> "Accepted next policy π_(i+1)"; No -> "Keep π_i". The old-support/nominal evaluation input should be labelled briefly. Most important: the dashed "Next iteration" arrow from Accepted next policy MUST end with its arrowhead on the BLUE "Current jumping policy π_i" block in panel (a), not on the section heading in (c). Route it around an outside margin or through a clean labelled pair of matching ports. No arrow from Keep π_i to the explorer reward box: keeping the controller is a policy-selection decision, not its reward signal.

6. Correct explorer learning arrows: "Delayed quality feedback" must start at the outcome labels of the re-test in (b) and end at the orange "Explorer PPO update" box in (c), not end at the tube panel title. The per-current-policy Visited ledger in (b)/(c) supplies novelty to the same update. The dashed "Updated explorer parameters (Eφ)" arrow MUST end on the AMBER "Residual explorer Eφ" network in (a), not on the Privileged history input box. Keep this feedback route visually distinct from the accepted-controller route. Use labelled matching input/output ports for these two feedback signals if needed to avoid tangled long arrows.

7. In panel (d), keep the excellent single-jump trajectory bundle with a short stable tail. Remove "x = 0" from the start label; just write "Fixed start". x denotes forward position, and the world-coordinate starting point is not being reset to zero here. Keep no numeric axis ticks. The white or gray region labelled Unexplored should be mostly blank, with no dashed complete trajectories drawn through it: unknown trajectories have not been measured. Keep thin actual-style schematic successful curves and small failed-state crosses. Do not fill a convex hull.

8. Remove the arrow from "Tube archive = successful prefix + continuation witnesses" to "Final controller π*". These are independent outputs, not an archive-to-policy conversion. Keep the archive card connected to successful witnesses. Connect the final controller card to Accepted next policy by a short labelled terminal route/port "After final iteration: freeze". Beneath it keep "Independent perturbed-jump evaluation" and intended metrics only, no result values.

9. Retain four event locations as alternatives for different rollouts, three different residual bars, 0.06 s pulse duration, 0.5 s stable landing criterion, two learning objectives, fresh simulation from reset states, old/pending mixture, current-policy novelty ledger, and complete-state snapshot preservation. Retain the methods-only presentation: no implemented/planned badges, no measured improvement curves, no claim that final π* automatically controls every historical witness.

10. Use ample white gutters. Labels and arrowheads must stay separated from boxes and titles. No arrow is decorative; every arrow must have the source and destination specified above. Keep the overall figure attractive and usable as a robotics manuscript method overview.
